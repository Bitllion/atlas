# Atlas AGENTS.md

## 项目介绍

你正在参与开发 Atlas 项目。

Atlas 是一个面向 AI 数据中心、高性能计算环境以及企业级基础设施环境的智能运营管理平台。其核心目标是：

> 构建 AI 基础设施的数字化底座，实现从资源建模、资产管理、运维运营到自动化能力扩展的一体化管理。

Atlas 不是简单的资产管理系统，而是基于 Infrastructure Object Model（基础设施对象模型）构建的综合运营平台。

Atlas 必须支持真实企业环境：

- 自有数据中心
- 客户数据中心
- 第三方托管环境
- 多组织、多地点、多权限场景

系统设计必须考虑：

- 资产所有权
- 部署位置
- 使用方
- 管理范围
- 数据来源差异

## 一、开发角色定位

你作为 Atlas 项目的 AI 软件工程助手，需要：

- 理解 Atlas 产品定位
- 遵循项目架构设计
- 根据需求实现功能
- 保持代码质量
- 避免破坏核心模型
- 优先考虑长期可维护性

开发过程中不要只关注当前功能实现，还需要考虑：

- 数据模型是否合理
- 是否影响未来扩展
- 是否符合 Atlas 整体架构

## 二、开发前必须阅读

开始任何开发任务前，必须优先阅读：

- `docs/00-产品定义.md`

根据任务类型阅读对应设计文档：

- 业务功能：`docs/01-Atlas业务架构设计.md`
- 资源模型：`docs/02-基础设施对象模型.md`
- 数据库相关：`docs/` 下的数据库设计文档
- 接口相关：`docs/` 下的 API 设计文档

如果对应文档不存在，需要先补充设计，而不是直接编码。

## 三、核心开发原则

### 1. 模型优先（Model First）

开发必须遵循：

```text
业务需求
  ↓
模型设计
  ↓
数据设计
  ↓
接口设计
  ↓
代码实现
```

不要从页面需求直接创建数据库表并快速实现。

### 2. 不破坏核心模型

Atlas 的核心价值是基础设施数字模型。任何开发不能破坏：

- Object 模型
- Relationship 模型
- Specification 模型
- Data Source 模型
- History 模型

如果需求与已有模型冲突，优先优化模型，而不是增加临时字段。

### 3. 区分对象、资产和业务记录

不要混淆以下概念：

- Infrastructure Object：真实世界中的基础设施实体，如 Server、GPU、NIC、Rack、CDU
- Asset Record：资产管理信息，如资产编号、采购信息、供应商、维保信息
- Business Record：业务过程产生的数据，如工单、巡检记录、维修记录

三者不能混为一谈。

## 四、数据设计规范

### 1. 优先使用通用模型

避免为单个设备创建完全独立的模型，例如 `gpu_table`、`server_table`、`cdu_table`。

推荐使用：

- `objects`
- `object_types`
- `relationships`
- `specifications`

### 2. 支持扩展字段

硬件设备差异较大，不要假设所有设备字段完全一致。模型应支持：

- 固定字段
- 扩展字段
- JSON 属性

例如 GPU 可能包含 HBM 容量、Firmware、UUID；NIC 可能包含 MAC、BDF、Firmware；CDU 可能包含 Cooling Capacity、Flow Rate。

### 3. 保留数据来源

Atlas 数据可能来自：

- 自动采集
- 人工录入
- 文件导入
- 厂商资料
- 客户反馈

所有重要数据需要考虑：

- 来源
- 时间
- 更新时间
- 可信度

不要默认所有数据都是实时采集。

## 五、业务边界要求

### 1. 不默认所有设备可访问

Atlas 管理的设备可能位于内部数据中心、客户数据中心或第三方机房。不要默认可以 SSH、执行命令或自动修改配置。

必须考虑：

- 管理权限
- 访问范围
- 客户授权
- 资产所有权与使用权分离
- 设备位置和管理范围

### 2. 自动化能力需受约束

Atlas 支持未来自动化能力，例如 PXE、硬件发现和 Firmware 管理，但自动化不是默认能力。

是否可以执行操作，需要根据以下条件判断：

- 设备位置
- 管理权限
- 网络环境
- 客户授权

## 六、后端开发规范

默认技术栈：

- Python
- FastAPI
- SQLAlchemy
- PostgreSQL

原则：

- 清晰模块划分
- 数据模型优先
- API 稳定

## 七、前端开发规范

默认技术栈：

- Vue 3
- TypeScript

页面设计需要：

- 面向业务用户
- 不暴露底层复杂实现
- 保持统一交互

## 八、Agent 开发规范

硬件采集 Agent 优先使用 Go，用于：

- Redfish
- IPMI
- SNMP
- `nvidia-smi`
- `lspci`
- `ethtool`

Agent 设计要求：

- 独立部署
- 稳定运行
- 异常可恢复
- 数据来源明确

## 九、API 设计规范

API 应围绕资源设计，例如：

```text
GET /api/v1/objects
GET /api/v1/objects/{id}
GET /api/v1/objects/{id}/relations
GET /api/v1/objects/{id}/specifications
```

避免根据页面创建接口，例如 `GET /api/get_gpu_detail_page`。

## 十、代码规范

代码必须：

- 可读
- 可维护
- 包含必要注释
- 避免重复代码

新增功能需要同步更新：

- 文档
- 数据模型说明
- API 说明

## 十一、禁止事项

禁止：

- 将 Atlas 做成简单资产管理系统
- 绕过核心模型快速开发
- 创建重复数据模型
- 为单页面需求增加临时结构
- 默认所有设备可自动控制
- 忽略客户环境限制

未经设计不得直接：

- 修改数据库核心结构
- 删除已有模型
- 改变数据含义

## 十二、Git 提交规范

Commit 信息建议使用以下格式：

- 新增功能：`feat: 新增资产入库流程`
- 修复问题：`fix: 修复对象关系查询问题`
- 重构：`refactor: 优化对象模型结构`
- 文档：`docs: 更新业务架构文档`

## 十三、最终目标

Atlas 的目标是：

> 成为 AI 数据中心基础设施统一运营管理平台。

所有开发决策必须服务于：

- 基础设施数字化
- 资产生命周期管理
- 运维效率提升
- 多环境资源管理
- 数据可信管理
- 长期扩展能力

设计优先级：

```text
产品定义
  ↓
架构设计
  ↓
核心模型
  ↓
业务需求
  ↓
页面需求
  ↓
代码便利性
```
